{
  "name": "luna",
  "version": "0.1.2",
  "main": "./index.js",
  "types": "./index.d.ts",
  "repository": "git@github.com:dominicwhittle/luna.git",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc --declaration",
    "watch": "npx tsc -w & yarn",
    "demos": "esbuild src/demo/*.ts --bundle --minify --watch --sourcemap --outdir=src/demo",
    "serve": "http-server ./src/demo --port 8080"
  },
  "devDependencies": {
    "csstype": "^3.0.9",
    "esbuild": "^0.13.3",
    "http-server": "^13.0.2",
    "typescript": "^4.4.3"
  }
}
