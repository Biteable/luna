{
  "name": "luna",
  "version": "0.1.2",
  "main": "index.js",
  "repository": "git@github.com:dominicwhittle/luna.git",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc -w & yarn",
    "demos": "esbuild demo/*.ts --bundle --minify --watch --sourcemap --outdir=demo",
    "serve": "http-server ./demo --port 8080"
  },
  "dependencies": {
    "csstype": "^3.0.9",
    "esbuild": "^0.13.3",
    "http-server": "^0.12.3",
    "typescript": "^4.2.2"
  }
}
